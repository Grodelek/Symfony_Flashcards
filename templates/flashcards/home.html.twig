{% extends "base.html.twig" %}

{% block body %}
    {{include('flashcards/navbar.html.twig')}}

    <div class="inner cover">
        <h1 class="cover-heading" style="text-align: center">Flashcards.</h1>
        <p class="lead" style="text-align: center">Flashcard is app focused on best user experience and simplicity.
            <br>Create new, edit existing ones, change status and download your own flashcards.</p>
    </div>

    <div class="d-flex flex-column align-items-center mt-4">
   <a class="btn btn-primary btn-lg m-3 card-btn" href="{{ path('cards_all') }}">View Flashcards</a>
   <br>
    </div>
{% endblock %}
